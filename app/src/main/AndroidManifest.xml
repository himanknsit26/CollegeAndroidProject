<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.himankrao.dillidarshan">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Main_2">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Restauarants"
            android:label="@string/resto" />
        <activity
            android:name=".TouristSpots"
            android:label="@string/tSpots" />
        <activity
            android:name=".ShoppingAttractions"
            android:label="@string/shop" />
        <activity
            android:name=".PlacesNearDelhi"
            android:label="@string/nearby" />
        <activity
            android:name=".EmergencyNumbers"
            android:label="@string/emergency" />
    </application>

</manifest>